apiVersion: v1
kind: ConfigMap
metadata:
  name: nginx-conf
data:
  nginx.conf: |
server {
    listen 8000;
    root /homework
    autoindex on;

    location / {
        try_files $uri/ =404;
    }
}   